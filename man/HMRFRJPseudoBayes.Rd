% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HMRFRJPseudoBayes.R
\name{HMRFRJPseudoBayes}
\alias{HMRFRJPseudoBayes}
\title{Reversible-Jump sampler for Hidden MRF with Gaussian emissions}
\description{
An R6 class combining \link{HMRFPseudoBayes} and \link{MRFRJPseudoBayes}: a
Metropolis-within-Gibbs sampler that simultaneously infers the latent
categorical field, the Gaussian emission parameters, the MRF parameter
array, and the interaction structure via Reversible-Jump MCMC.
}
\details{
The model is a Hidden MRF with Gaussian emissions:
\deqn{z \mid \theta \sim \text{MRF}(\theta, \text{mrfi})}
\deqn{y_{ij} \mid z_{ij} = k \sim N(\mu_k, \sigma^2_k)}

Conjugate priors are placed on the emission parameters:
\deqn{\mu_k \sim N(0, \sigma^2_\mu), \quad 1/\sigma^2_k \sim \text{Gamma}(\alpha_1, \alpha_2)}

At each MCMC iteration the following updates are performed in order:
\enumerate{
\item \strong{Emission parameters} (\code{mu_k}, \code{sigma2_k}): conjugate full-conditional
Gibbs update given the current latent field and observations.
\item \strong{Latent field} (\code{z}): single-cycle Gibbs sweep using
\code{\link[=inner_gibbs_conditional]{inner_gibbs_conditional}}, with
conditional weights that combine MRF prior (via current \code{theta}) and
Gaussian emission likelihoods.
\item \strong{Interaction structure and theta} (\code{M}, \code{theta}): one Reversible-Jump
MH step (within / birth / death / swap / jump) targeting the
pseudo-posterior of \verb{(theta, M)} given the newly sampled \code{z}.
}

This class inherits all methods of \link{MRFRJPseudoBayes}, including
\verb{$summary()} (theta + inclusion probabilities), \verb{$plot_mrfi()}, and
\verb{$samples_mrfi}.
}
\section{Super classes}{
\code{\link[mrf2dbayes:MRFPseudoBayes]{mrf2dbayes::MRFPseudoBayes}} -> \code{\link[mrf2dbayes:MRFRJPseudoBayes]{mrf2dbayes::MRFRJPseudoBayes}} -> \code{HMRFRJPseudoBayes}
}
\section{Active bindings}{
\if{html}{\out{<div class="r6-active-bindings">}}
\describe{
\item{\code{y}}{The observed continuous field (read-only).}

\item{\code{mu}}{Current emission means (read-only numeric vector, length C+1).}

\item{\code{sigma2}}{Current emission variances (read-only, length C+1).}

\item{\code{sdpriormu}}{SD of the Gaussian prior on each \code{mu_k} (read-only).}

\item{\code{alpha1}}{Shape of the Gamma prior on precision (read-only).}

\item{\code{alpha2}}{Rate of the Gamma prior on precision (read-only).}

\item{\code{samples_pars}}{A \code{\link[tibble:tibble]{tibble::tibble}} with columns \code{t}, \code{k}, \code{par}, and \code{value}
containing all collected samples of emission parameters (\code{mu} and
\code{sigma2}). Returns \code{NULL} if \verb{$run()} has not been called yet.}

\item{\code{zprobs}}{Posterior label probability array with dimensions
\code{c(nrow(y), ncol(y), C+1)}. Entry \verb{[i, j, k+1]} is the empirical
posterior probability that \code{z[i,j] == k}. Returns \code{NULL} before
the first \verb{$run()} call.}

\item{\code{zmap}}{MAP classification matrix with the same dimensions as \code{y}. Each entry
contains the label \code{k} in \verb{0, ..., C} with the highest empirical
posterior probability. Returns \code{NULL} before the first \verb{$run()} call.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-HMRFRJPseudoBayes-new}{\code{HMRFRJPseudoBayes$new()}}
\item \href{#method-HMRFRJPseudoBayes-run}{\code{HMRFRJPseudoBayes$run()}}
\item \href{#method-HMRFRJPseudoBayes-summary_pars}{\code{HMRFRJPseudoBayes$summary_pars()}}
\item \href{#method-HMRFRJPseudoBayes-plot}{\code{HMRFRJPseudoBayes$plot()}}
\item \href{#method-HMRFRJPseudoBayes-print}{\code{HMRFRJPseudoBayes$print()}}
\item \href{#method-HMRFRJPseudoBayes-clone}{\code{HMRFRJPseudoBayes$clone()}}
}
}
\if{html}{\out{
<details open><summary>Inherited methods</summary>
<ul>
<li><span class="pkg-link" data-pkg="mrf2dbayes" data-topic="MRFRJPseudoBayes" data-id="plot_mrfi"><a href='../../mrf2dbayes/html/MRFRJPseudoBayes.html#method-MRFRJPseudoBayes-plot_mrfi'><code>mrf2dbayes::MRFRJPseudoBayes$plot_mrfi()</code></a></span></li>
<li><span class="pkg-link" data-pkg="mrf2dbayes" data-topic="MRFRJPseudoBayes" data-id="summary"><a href='../../mrf2dbayes/html/MRFRJPseudoBayes.html#method-MRFRJPseudoBayes-summary'><code>mrf2dbayes::MRFRJPseudoBayes$summary()</code></a></span></li>
</ul>
</details>
}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-new"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-new}{}}}
\subsection{Method \code{new()}}{
Create a new \code{HMRFRJPseudoBayes} sampler.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$new(
  y,
  mrfi,
  family,
  C,
  sdprior = 10,
  sdkernel = 0.005,
  sdbirth = 0.05,
  logpenalty = NULL,
  kernel_probs = c(4, 1, 1, 1, 1),
  init_included = "zero",
  init_theta = "zero",
  sdpriormu = 10,
  alpha1 = 0.001,
  alpha2 = 0.001,
  z0 = NULL,
  mu0 = NULL,
  sigma2_0 = NULL,
  verbose = interactive()
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{y}}{A numeric matrix of observed continuous values.}

\item{\code{mrfi}}{An \code{mrfi} object specifying the \strong{maximal} interaction
structure; the algorithm can select any subset of its positions.}

\item{\code{family}}{A character string naming the parameter restriction family.}

\item{\code{C}}{Non-negative integer; the latent field takes values \verb{0, ..., C}.}

\item{\code{sdprior}}{Standard deviation of the Gaussian prior on active
\code{theta} entries. Defaults to \code{10}.}

\item{\code{sdkernel}}{Standard deviation of the \code{within}-move proposal for
\code{theta}. Defaults to \code{0.005}.}

\item{\code{sdbirth}}{Standard deviation of birth and jump-add proposals.
Defaults to \code{0.05}.}

\item{\code{logpenalty}}{Log-penalty per active position. Defaults to
\code{log(nrow(y) * ncol(y))}.}

\item{\code{kernel_probs}}{Numeric vector of length 5 with unnormalised weights
for \verb{(within, swap, death, birth, jump)}. Defaults to \code{c(4,1,1,1,1)}.}

\item{\code{init_included}}{Initial active set: \code{"zero"} (default), \code{"full"},
or a logical vector of length equal to the number of positions in
\code{mrfi}.}

\item{\code{init_theta}}{Initial \code{theta}: \code{"zero"} (default), \code{"pl"}, or a
numeric vector of length \code{fdim}.}

\item{\code{sdpriormu}}{Standard deviation of the Gaussian prior on each
emission mean \code{mu_k}. Defaults to \code{10}.}

\item{\code{alpha1}}{Shape parameter of the Gamma prior on each precision
\code{1/sigma2_k}. Defaults to \code{0.001}.}

\item{\code{alpha2}}{Rate parameter of the Gamma prior on precision.
Defaults to \code{0.001}.}

\item{\code{z0}}{Integer matrix; initial latent field. If \code{NULL}, values are
computed via the EM algorithm (\link[mrf2d:fit_ghm]{mrf2d::fit_ghm}).}

\item{\code{mu0}}{Numeric vector of length \code{C+1}; initial emission means.
Computed via EM when \code{NULL} and \code{z0} is also \code{NULL}.}

\item{\code{sigma2_0}}{Numeric vector of length \code{C+1}; initial emission
variances. Computed via EM when \code{NULL} and \code{z0} is also \code{NULL}.}

\item{\code{verbose}}{If \code{TRUE}, prints progress during EM initialisation.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-run"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-run}{}}}
\subsection{Method \code{run()}}{
Run the sampler.

Each iteration performs: (1) conjugate update of emission parameters,
(2) Gibbs sweep of the latent field, (3) one Reversible-Jump MH step
on \verb{(theta, M)} using the newly sampled field. Samples are appended
to any previously collected samples.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$run(nsamples, verbose = interactive())}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{nsamples}}{Number of iterations to run.}

\item{\code{verbose}}{If \code{TRUE}, prints iteration progress.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
The sampler itself, invisibly (allows chaining).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-summary_pars"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-summary_pars}{}}}
\subsection{Method \code{summary_pars()}}{
Compute posterior summary statistics for the emission parameters.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$summary_pars(burnin = 0.25)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{burnin}}{Fraction (if \verb{< 1}) or absolute number of initial samples
to discard. Defaults to \code{0.25}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A \code{\link[tibble:tibble]{tibble::tibble}} with columns \code{k}, \code{par}, \code{q025}, \code{mean},
\code{q975}, and \code{sd}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-plot"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-plot}{}}}
\subsection{Method \code{plot()}}{
Plot MCMC traces, neighbourhood structure, or posterior label maps.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$plot(what = "pars", burnin = 0, thin = 50L)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{what}}{One of:
\itemize{
\item \code{"pars"} — trace plots of \code{mu} and \code{sigma2} (default);
\item \code{"theta"} — trace plot of \code{theta} (inherits RJ-style segment plot);
\item \code{"mrfi"} — neighbourhood coloured by inclusion probability;
\item \code{"zprobs"} — heatmaps of posterior label probabilities.
}}

\item{\code{burnin}}{Burn-in applied when \code{what} is \code{"theta"} or \code{"mrfi"}.}

\item{\code{thin}}{Thinning applied when \code{what = "theta"}. Defaults to \code{50}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A \code{ggplot} object.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-print"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-print}{}}}
\subsection{Method \code{print()}}{
Print a compact description of the sampler.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$print(...)}\if{html}{\out{</div>}}
}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-HMRFRJPseudoBayes-clone"></a>}}
\if{latex}{\out{\hypertarget{method-HMRFRJPseudoBayes-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{HMRFRJPseudoBayes$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
